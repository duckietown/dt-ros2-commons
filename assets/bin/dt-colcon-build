#!/usr/bin/env bash

set -e

if [ "$#" -ne 1 ]; then
    >&2 echo "ERROR: You need to pass a workspace path as an argument to dt-colcon-build.";
    exit 1;
fi

WORKSPACE=$1

cd ${WORKSPACE} && colcon build --symlink-install
